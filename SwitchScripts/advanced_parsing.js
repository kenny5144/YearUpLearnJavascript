
function parseAndDisplay(good){
    let name = good;

    let theName = name.indexOf(" ")
    let firstName = name.substring(0,theName)
    let midName = name.slice
    let lastName = name.slice(theName)
    console.log(firstName)

}
parseAndDisplay("Keh" )
